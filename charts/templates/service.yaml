apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitwarden-password-manager-eso.fullname" . }}
  labels:
    {{- include "bitwarden-password-manager-eso.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitwarden-password-manager-eso.selectorLabels" . | nindent 4 }}